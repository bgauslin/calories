user-values
  display block
  grid-area user-values
  margin 1rem 0 0

  fieldset
    border 0
    margin 0 0 1rem
    opacity 1
    padding 0
    transition opacity transition

  fieldset[disabled]
    opacity .3
    pointer-events none

  // Tweak a few <fieldset> layouts based on their content.
  #sex
    margin-bottom 1.5rem

  #measurements
    margin-bottom .5rem

  h2
    display flex
    font-size 1rem
    margin 0 0 .5rem

  h2 > span
    color var(--text-2-color)
    margin-left auto

  // Pull 'Sex' heading out of the viewport since its options make this obvious.
  #sex h2
    left -100vw
    position absolute

  ul
  li
    align-items center
    display flex
    padding 0

  ul
    margin 0

  #measurements ul
    flex-wrap wrap

  li
    margin 0 0 .75rem

    &.weight
    &.age
      flex 0 0 100%

    &.feet::before
      content 'Height'
      width label-width

    &.weight::after
    &.age::after
      color var(--text-2-color)
      margin-left .5em
      pointer-events none

    &.weight::after
      content 'lbs.'

    &.age::after
      content 'yrs.'

  label
    display flex

    &:focus
      outline none

    [no-touch] &:hover
      cursor pointer

    &:focus > input[type] + span
    [no-touch] &:hover > input[type] + span
      color var(--text-1-color)
      transform scale(1.1)

    &[for=feet]
    &[for=inches]
      color var(--text-2-color)
      margin 0 1rem 0 .5rem
      order 2
      text-transform lowercase

    &[for=age]
    &[for=weight]
      width label-width

  label > span
    transition color transition, transform transition

  input[type=text]
    background var(--background-2)
    border 2px solid var(--background-0)
    border-radius 6px
    box-sizing content-box
    color var(--text-1-color)
    font-size 1em
    height input-height
    padding 0 .5rem
    text-align right
    width 3rem

  input[type=radio]
    & + span
      align-items center
      border-radius 2px
      color var(--text-2-color)
      display flex
      justify-content center
      pointer-events none
      width 100%

    &:checked + span
      color var(--text-1-color)

  [no-js] &
    display none
