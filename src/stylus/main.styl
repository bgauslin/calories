main
  display grid
  grid:
    'user-values' \
    'zig-zag' 1fr
  grid-area main
  justify-self center
  max-width CONTENT_MAX_WIDTH
  padding 0 var(--site-padding) env(safe-area-inset-bottom)

[no-js] main
  align-items center
  grid unset
