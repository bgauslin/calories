main
  display grid
  gap var(--main-gap)
  grid var(--main-grid)
  grid-area main
  justify-self center
  max-inline-size var(--main-max-width)
  padding-block 0 env(safe-area-inset-bottom)

[no-js] main
  align-items center
  grid unset
