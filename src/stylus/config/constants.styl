@import 'functions'

// @type Hash
Branding = {
  RED: #b22222 // firebrick
  ORANGE: #ff7a00
}

// @type Hash
LinkColor = {
  LIGHT: #0070c9
  DARK: #008eff
}

// @const String
BUTTON_HEIGHT = rem(44)

// @const String
DARK_GREY = #222

// @const String
CONTENT_MAX_WIDTH = rem(400)

// @const String
INPUT_HEIGHT = 2rem

// @const String
TEXT_OPACITY = .7

// -----------------------------------------------------------------------------
// Calculated constants and breakpoints

// @type Hash
Breakpoint = {
  SMALL:  'screen and (min-width: %s)' % rem(420)
  MEDIUM: 'screen and (min-width: %s)' % rem(640)
  LARGE:  'screen and (min-width: %s)' % rem(960)
}
