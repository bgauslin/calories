@import 'functions'

// @type Hash
Branding = {
  RED: #b22222 // firebrick
  ORANGE: #ff7a00
}

// @type Hash
Theme = {
  DARK: #282c35
}

// @const String
CONTENT_MAX_WIDTH = px_to_rem(400)

// @const String
GRID_GAP = px_to_rem(16)

// @const String
GRID_GAP_MEDIUM = px_to_rem(24)

// @const String
INPUT_HEIGHT = 2rem

// @const String
TEXT_OPACITY = .6

// -----------------------------------------------------------------------------
// Calculated constants and breakpoints

// @type Hash
Breakpoint = {
  SMALL:  'screen and (min-width: %s)' % px_to_rem(420)
  MEDIUM: 'screen and (min-width: %s)' % px_to_rem(640)
  LARGE:  'screen and (min-width: %s)' % px_to_rem(960)
}
